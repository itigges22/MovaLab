# =====================================================
# MOVALAB ENVIRONMENT CONFIGURATION
# =====================================================
# This file configures which database MovaLab connects to.
# Copy to .env.local and uncomment the appropriate section.
#
# IMPORTANT: Only ONE database configuration should be active!
#
# Two modes available:
#   1. LOCAL DEMO MODE - Docker Supabase (for demos, local testing)
#   2. CLOUD MODE - Cloud Supabase (for production/Vercel)

# =====================================================
# MODE 1: LOCAL DEMO MODE (Docker Supabase)
# =====================================================
# Use this for:
#   - Public demos
#   - Local development
#   - Testing without affecting production
#
# Requirements:
#   - Docker Desktop must be installed and running
#   - Run: npm run dev:demo (auto-starts Docker)
#
# Features in demo mode:
#   - Quick-login buttons (no signup required)
#   - Delete/remove actions disabled
#   - Superadmin setup hidden
#   - Pre-loaded demo data

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Enable demo mode UI features (auto-set by npm run dev:demo)
# NEXT_PUBLIC_DEMO_MODE=true

# =====================================================
# MODE 2: CLOUD MODE (Cloud Supabase)
# =====================================================
# Use this for:
#   - Production deployments (Vercel)
#   - Staging environments
#   - Development against real data
#
# To use cloud mode:
#   1. Comment out MODE 1 section above
#   2. Uncomment and fill in your cloud credentials below
#   3. Run: npm run dev:cloud (no Docker needed)
#
# For Vercel deployment:
#   - Set these as environment variables in Vercel dashboard
#   - Do NOT commit real credentials to git!

# NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-cloud-publishable-key

# For production DEMO on Vercel (with demo UI features):
# NEXT_PUBLIC_DEMO_MODE=true

# =====================================================
# APP CONFIGURATION (Same for both modes)
# =====================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
LOG_LEVEL=debug
NODE_ENV=development

# =====================================================
# FIRST-TIME SETUP (Required for cloud deployments)
# =====================================================
# This secret is used to create the first superadmin account.
# Only needed for fresh cloud deployments (not needed for local Docker).
#
# How to generate:
#   openssl rand -hex 32
#
# Setup process:
#   1. Link your Supabase project:  supabase link --project-ref YOUR_PROJECT_REF
#   2. Push database schema:        supabase db push
#   3. Set SETUP_SECRET below (and in Vercel if deploying there)
#   4. Sign up for an account at your app URL
#   5. Go to /setup?key=YOUR_SETUP_SECRET
#   6. You'll become the superadmin
#
# After setup completes, this secret is no longer used (but keep it secure!)
# See docs/setup/FIRST_TIME_SETUP.md for the full guide.

# SETUP_SECRET=your-random-secret-key-here

# =====================================================
# OPTIONAL: RATE LIMITING (Production recommended)
# =====================================================
# Get credentials from: https://console.upstash.com

# UPSTASH_REDIS_REST_URL=your_upstash_redis_url
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token
# ENABLE_RATE_LIMIT=false

# =====================================================
# QUICK START COMMANDS
# =====================================================
#
# LOCAL DEMO MODE:
#   npm run dev:demo
#   - Auto-starts Docker Supabase
#   - Enables demo mode UI
#   - Open http://localhost:3000
#   - Login: exec@test.local / Test1234!
#
# CLOUD MODE:
#   npm run dev:cloud
#   - Uses cloud credentials from this file
#   - No Docker required
#   - Good for production testing
#
# STOP DOCKER:
#   npm run docker:stop
#
# RESET DEMO DATA:
#   npm run docker:seed
